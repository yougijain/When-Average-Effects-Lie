# Pinned to the versions the committed RESULTS.md and figures/ were generated
# with. The pins are deliberate: the estimators in Layers 3-5 are exactly
# reproducible, but the gradient-boosted uplift models in Layer 6 can shift a
# Qini point or two across scikit-learn releases, so an unpinned rerun will not
# reproduce RESULTS.md digit-for-digit. Tested on Python 3.13.
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
statsmodels==0.14.6
scikit-learn==1.9.0
matplotlib==3.11.0

# If you need to install against an older stack, these are the API floors the
# script actually requires (np.trapezoid falls back to np.trapz at runtime):
#   pandas>=2.0  numpy>=1.24  scipy>=1.10  statsmodels>=0.14
#   scikit-learn>=1.2 (OneHotEncoder sparse_output)  matplotlib>=3.7
