# Pinned to the versions the committed RESULTS.md and figures/ were generated
# with (Python 3.13.12, x86-64 Linux). Layers 1-5 reproduce exactly under any
# compatible stack. Layer 6's gradient-boosted uplift models are the one part
# that has been observed to give different numbers in a different environment,
# so they are what the pins protect.
#
# Needs Python 3.12 or newer: numpy 2.5 and scipy 1.18 have no wheels for 3.11.
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
statsmodels==0.14.6
scikit-learn==1.9.0
matplotlib==3.11.0

# If you need to install against an older stack, these are the API floors the
# script actually requires (np.trapezoid falls back to np.trapz at runtime):
#   pandas>=2.0  numpy>=1.24  scipy>=1.10  statsmodels>=0.14
#   scikit-learn>=1.2 (OneHotEncoder sparse_output)  matplotlib>=3.7
